# AI 功能评审模拟器（Feature Review Simulator）

## 产品需求说明 & 完整技术方案

|版本|v1.0|
|---|---|
|日期|2026-03|
|作者|AI PM Team|
|状态|评审中|
---

## 1. 产品概述

### 1.1 背景与痛点

AI 产品经理每天需要主持多场需求评审会，在会议前往往缺乏充分的压力测试机会。当 PRD 提交后才发现被工程师、老板、法务各方轮番挑战，不仅造成沟通成本，也拉长了需求落地周期。

AI 功能评审模拟器旨在解决以下核心痛点：

- PRD 提交前缺少多视角预审，盲点多

- 需求评审会上被临时质疑，应对被动

- 无法高效沉淀跨角色的质疑问题库

- 迭代改版后难以追踪各方意见的收敛情况

### 1.2 产品定位

面向 AI 产品经理的需求评审辅助工具，通过大模型驱动的多角色模拟，在 PRD 正式评审前帮助用户提前发现盲点、打磨方案，最终提升评审通过率。

### 1.3 产品口号

"上会之前，先过这四关。"

### 1.4 目标用户

|用户类型|描述|核心诉求|
|---|---|---|
|主要用户|AI 产品经理 / 产品负责人|上会前快速发现盲点，提高评审通过率|
|次要用户|普通产品经理|降低被技术/法务挑战的风险|
|潜在用户|创业者 / 独立开发者|独立验证产品想法的可行性|
---

## 2. 产品迭代路线

### 2.1 两阶段策略总览

采用「快速验证 → 完整产品」两步走策略，降低早期投入风险，以真实用户反馈驱动后期建设优先级。

|阶段|Phase 1 — Demo 验证|Phase 2 — 完整产品|
|---|---|---|
|目标|验证核心评审体验是否有价值|构建可商业化的完整 SaaS 产品|
|周期|1～2 周|6～10 周|
|技术栈|纯前端单页应用，零后端|前端 + Node.js 后端 + 数据库|
|核心功能|文档上传 → 多角色并发评审 → 结果展示|注册登录 + 文档管理 + 多轮辩驳 + 报告导出|
|成功标准|10 位目标用户试用，NPS ≥ 40|付费转化率 ≥ 5%，月留存 ≥ 60%|
---

## 3. Phase 1 详细需求（Demo 阶段）

### 3.1 功能范围

#### 3.1.1 文档上传解析

- 支持上传 Word（.docx）和 PDF 文件

- 文件大小限制：10MB

- 前端本地解析，不上传至任何服务器

- 解析完成后提取纯文本内容用于 prompt 拼装

- 支持直接粘贴文本作为备选输入方式

#### 3.1.2 角色选择

- 提供 4 个预设评审角色（默认全选）

- 用户可取消勾选任意角色（至少保留 1 个）

- 角色卡片展示：emoji + 名称 + 关注维度描述

#### 3.1.3 并发评审引擎

- 用户点击「开始评审」后，并发调用所有选中角色

- 使用 Promise.all 并发，各角色独立请求，互不阻塞

- 谁先返回谁先渲染，提供实时流式加载感

- 每个角色的 system prompt 独立设计，强调极端性格

#### 3.1.4 结果展示

- 每个角色以独立卡片展示，含角色标识、5 条质疑问题

- 所有角色完成后，AI 汇总提炼 3～4 条核心共性风险

- 底部提供「修改方案后重新评审」入口（迭代闭环）

### 3.2 非功能需求

- 总评审耗时 ≤ 10 秒（4 角色并发）

- 支持 Anthropic Claude API（用户自行输入 API Key）

- 纯前端部署，可托管至 GitHub Pages / Vercel / Netlify

- 界面支持中文，兼容 Chrome / Safari / Edge 最新版

### 3.3 Phase 1 技术架构

#### 3.3.1 整体架构

单页应用（SPA）+ 直连 Anthropic API，零后端，零数据库。

|模块|技术选型|
|---|---|
|前端框架|React 18 + Vite（或纯 HTML/JS 单文件）|
|文档解析|mammoth.js（Word） + PDF.js（PDF）|
|AI 调用|Anthropic API v1/messages，直接从浏览器调用，带 dangerous-direct-browser-access 头|
|并发控制|Promise.all，各角色独立 fetch，独立渲染|
|部署|Vercel / Netlify 静态托管，一键部署|
#### 3.3.2 文档解析方案

Word 文件解析使用 mammoth.js，在浏览器内完成，输出 Markdown 文本。PDF 使用 PDF.js 提取每页文本后拼接。解析流程如下：

1. 用户选择文件 → FileReader 读取为 ArrayBuffer

2. 根据文件扩展名分发至 mammoth.js 或 PDF.js

3. 提取纯文本，截取前 3000 字符作为 PRD 内容（防止超出 token 上限）

4. 将文本注入各角色的 user message 中

#### 3.3.3 Prompt 设计原则

每个角色的 system prompt 遵循以下设计原则：

- 强调极端性格：不是「普通用户」，是「极度挑剔的用户」

- 明确输出格式：每条质疑以「- 」开头，一条一行，5 条

- 聚焦本角色关注维度，不跨维度发言

- 使用第一人称，语气真实，贴近评审现场

#### 3.3.4 核心代码骨架

```JavaScript

// 并发调用示例
const results = await Promise.all(
  selectedRoles.map(role => callClaude(apiKey, role.systemPrompt, prdText))
);

// 角色返回即渲染（非等全部完成）
roles.forEach(async (role) => {
  const result = await callClaude(apiKey, role.prompt, prdText);
  renderCard(role, result);  // 立即渲染
});
```

---

## 4. Phase 2 详细需求（完整产品）

### 4.1 完整用户旅程

Phase 2 产品的完整用户旅程分为 5 个阶段：

|#|阶段|用户行为|系统响应|
|---|---|---|---|
|1|注册 / 登录|邮箱注册 → 验证邮件 → 激活账号|发送验证码邮件，写入用户数据库|
|2|上传文档|在首页上传 Word / PDF 文件|解析文档，提取文本，存储至云端|
|3|发起评审|选择角色，点击「开始评审」|并发调用大模型，流式渲染各角色回答|
|4|多轮辩驳|针对每个角色的质疑进行回复/辩驳|角色基于上下文继续质疑，直至「同意」|
|5|生成报告|所有角色通过后，导出评审报告|生成结构化 PDF/Word 报告，可归档|
### 4.2 功能模块详细说明

#### 4.2.1 身份认证模块

- 注册：邮箱 + 密码，发送 6 位验证码，5 分钟有效

- 登录：邮箱密码登录 + 记住登录状态（JWT，7 天有效）

- 第三方登录（二期）：Google OAuth / GitHub OAuth

- 密码找回：邮箱验证码重置

- 安全要求：密码 bcrypt 加密存储，rate limiting 防爆破

#### 4.2.2 文档管理模块

- 支持格式：.docx、.pdf，单文件 ≤ 20MB

- 文档列表：展示历史上传文档，含文件名、上传时间、评审状态

- 文档解析：服务端解析文本，结果缓存，避免重复解析

- 文档状态：待评审 / 评审中 / 评审通过 / 评审未通过

- 删除文档：支持软删除，30 天内可恢复

#### 4.2.3 评审引擎模块

- 4 个内置评审角色，系统 prompt 由后端维护，不暴露给前端

- 并发调用：服务端并发请求大模型 API，通过 SSE 流式推送给前端

- 进度展示：每个角色有独立的加载状态（等待中 / 生成中 / 已完成）

- 错误处理：单个角色失败不影响其他角色，支持单独重试

- 每次评审结果持久化存储，可随时查看历史

#### 4.2.4 多轮辩驳模块（核心创新点）

这是产品最核心的差异化功能。每个角色维持独立的对话上下文，用户可以针对该角色的质疑进行回复，角色会基于历史对话继续追问或最终表示认可。

**辩驳状态机**：

- 初始状态：「质疑中」— 角色提出 5 个问题

- 用户回复后：角色重新评估，可能进入「部分认可」「持续质疑」「已通过」三种状态

- 已通过条件：角色在回复中明确表示对本次方案无进一步质疑（AI 判断）

- 全部通过：4 个角色全部进入「已通过」状态，评审正式通过，可生成报告

**上下文管理策略**：

- 每个角色维护独立的 messages 数组（含 system prompt + 完整对话历史）

- 超长对话（>10 轮）自动摘要历史，防止 token 超限

- 对话历史持久化，支持中途退出后继续辩驳

#### 4.2.5 评审报告模块

- 触发条件：所有选中角色均通过，用户主动点击「生成报告」

- 报告内容：PRD 摘要 + 各角色完整对话记录 + 最终共识结论

- 导出格式：PDF 和 Word（.docx）两种格式

- 报告归档：云端保存，可分享链接（可选公开/私密）

### 4.3 4 大评审角色系统设计

|角色|性格设定|关注维度|通过标准|
|---|---|---|---|
|😤 挑剔用户|极度挑剔，容易沮丧|使用流程、边界 Case、认知负担、用户预期差距|确认核心流程清晰，主要边界 Case 有处理方案|
|🧑‍💻 保守工程师|极度保守，低估进度|技术可行性、工期评估、系统耦合、数据边界|技术方案可落地，工期评估合理，无明显技术债|
|💼 ROI 老板|只看数据和回报|商业价值、机会成本、优先级、成功指标|有量化成功指标，商业价值说清楚了|
|⚖️ 合规律师|风险厌恶，逐条审查|数据授权、隐私合规、PIPL/GDPR、法律风险|数据使用合规，有明确的用户授权机制|
---

## 5. Phase 2 技术架构方案

### 5.1 整体架构

采用前后端分离架构，后端负责身份认证、文档存储、AI 调用代理和对话历史管理；前端负责交互展示。后端屏蔽 API Key，保障安全性。

|层次|技术选型|说明|
|---|---|---|
|前端|React 18 + TypeScript + Vite|SPA，Tailwind CSS 样式，Zustand 状态管理|
|后端|Node.js + Express / Fastify|RESTful API + SSE 流式推送|
|数据库|PostgreSQL + Prisma ORM|用户、文档、评审会话、对话历史|
|文件存储|Cloudflare R2 / AWS S3|原始文档存储，解析后文本存 DB|
|AI 服务|Anthropic Claude API|后端统一调用，API Key 不暴露前端|
|认证|JWT + Resend（邮件服务）|无状态 Token，邮箱验证码通过 Resend 发送|
|部署|Vercel（前端） + Railway（后端）|低成本，支持 SSE，易于扩展|
### 5.2 数据库设计

核心数据模型共 5 张表：

#### users 用户表

|字段|类型|说明|
|---|---|---|
|id|UUID|主键|
|email|VARCHAR|唯一，用于登录和通知|
|password_hash|VARCHAR|bcrypt 哈希，不存明文|
|is_verified|BOOLEAN|邮箱是否已验证|
|created_at|TIMESTAMP|注册时间|
#### documents 文档表

|字段|类型|说明|
|---|---|---|
|id|UUID|主键|
|user_id|UUID FK|关联用户|
|filename|VARCHAR|原始文件名|
|file_url|TEXT|云存储 URL（原始文件）|
|parsed_text|TEXT|解析后纯文本，用于 prompt 构建|
|status|ENUM|pending / reviewing / passed / failed|
#### review_sessions 评审会话表

|字段|类型|说明|
|---|---|---|
|id|UUID|主键，也是 SSE 频道 ID|
|document_id|UUID FK|关联文档|
|roles_selected|JSON Array|本次选中的角色列表|
|overall_status|ENUM|in_progress / all_passed / partial|
|report_url|TEXT|生成报告的云存储 URL|
#### role_threads 角色对话线程表

|字段|类型|说明|
|---|---|---|
|id|UUID|主键|
|session_id|UUID FK|关联评审会话|
|role_key|VARCHAR|user / dev / boss / legal|
|messages|JSONB|完整对话历史 [{role, content}]|
|status|ENUM|challenging / partial / approved|
|round_count|INT|当前辩驳轮次|
### 5.3 核心 API 设计

|方法|路径|说明|备注|
|---|---|---|---|
|POST|/auth/register|用户注册|发送验证邮件|
|POST|/auth/verify|邮箱验证|验证码校验，返回 JWT|
|POST|/auth/login|用户登录|返回 JWT Token|
|POST|/documents/upload|上传文档|multipart，返回 document_id|
|GET|/documents|文档列表|当前用户的所有文档|
|POST|/sessions|创建评审会话|返回 session_id|
|GET SSE|/sessions/:id/stream|流式接收评审结果|Server-Sent Events，按角色分片推送|
|POST|/threads/:id/reply|用户辩驳回复|角色继续对话，SSE 流式返回|
|GET|/sessions/:id/report|生成/获取报告|触发报告生成或返回已有报告 URL|
### 5.4 SSE 流式推送设计

评审引擎使用 Server-Sent Events（SSE）实现实时流式推送，而非 WebSocket，原因是评审是单向推送场景，SSE 更简单且无需额外库。

**SSE 事件格式**：

```JavaScript

// 角色开始
data: {"type":"role_start","role":"user","status":"generating"}

// 内容分片（流式）
data: {"type":"content_chunk","role":"user","chunk":"- 用户在哪里"}

// 角色完成
data: {"type":"role_done","role":"user","status":"challenging"}

// 全部完成
data: {"type":"session_done","summary":"核心风险：..."}
```

---

## 6. 多轮辩驳核心逻辑

### 6.1 辩驳状态机

每个角色线程有 3 种状态，通过 AI 判断（非关键词匹配）进行状态转移：

|状态|含义|转移条件|
|---|---|---|
|challenging（质疑中）|角色仍有未解决的质疑|初始状态，或用户回复未能解决质疑|
|partial（部分认可）|角色认可部分回答，但仍有疑虑|用户回复解决了部分质疑|
|approved（已通过）|角色对本方案无进一步质疑|AI 判断角色在回复中表示认可|
### 6.2 通过判断 Prompt

每次角色回复后，系统额外调用一次轻量 AI 判断当前是否应进入 approved 状态：

```Plain Text

// 状态判断 prompt（附在角色 system prompt 后）
"在你的回复末尾，必须加上 JSON 状态标记：
{"status": "approved"} 表示你对该方案无进一步质疑；
{"status": "challenging"} 表示你仍有未解决的顾虑。
只有当你真正满意时才可以输出 approved。"
```

### 6.3 上下文管理

- 每个角色维护独立的 messages 数组，包含完整对话历史

- 对话轮次 ≥ 8 轮时，自动对历史消息进行摘要压缩，防止 token 超限

- 摘要由 AI 完成，保留关键质疑点和用户的核心回复

- 对话历史实时持久化到数据库，刷新页面不丢失

### 6.4 全部通过逻辑

- 前端监听每个角色的状态变化

- 当所有选中角色均进入 approved 状态时，触发「全部通过」庆祝动画

- 系统自动记录通过时间和总对话轮数

- 解锁「生成评审报告」按钮

---

## 7. 开发里程碑计划

### 7.1 Phase 1 里程碑（2 周）

|Week|阶段|交付物|
|---|---|---|
|W1 前 3 天|基础框架 + 文档解析|页面骨架、Word/PDF 解析、API Key 输入|
|W1 后 4 天|评审引擎 + 角色系统|4 个角色 prompt 设计、并发请求、结果卡片渲染|
|W2 前 3 天|风险汇总 + 迭代入口|AI 汇总提炼、重新评审功能、界面打磨|
|W2 后 4 天|用户测试 + 调优|10 位目标用户试用、收集反馈、prompt 调优、部署上线|
### 7.2 Phase 2 里程碑（8 周）

|周次|模块|核心交付|
|---|---|---|
|W1-W2|后端基础设施|数据库设计、用户认证（注册/登录/邮件验证）、基础 API|
|W3|文档管理|文件上传到 R2、服务端解析、文档列表页|
|W4-W5|评审引擎 + SSE|后端并发调用 AI、SSE 流式推送、前端实时渲染|
|W6|多轮辩驳|角色对话上下文、状态机、通过判断逻辑|
|W7|报告生成|PDF/Word 报告导出、云端归档、分享链接|
|W8|测试 + 上线|E2E 测试、性能优化、生产环境部署、Beta 用户邀请|
---

## 8. 成功指标（OKR）

|阶段|目标 (O)|关键结果 (KR)|衡量方式|
|---|---|---|---|
|Phase 1|验证核心体验有价值|NPS ≥ 40（10 位试用用户）|试用后问卷调研|
|Phase 1|验证核心体验有价值|≥ 7 位用户完成完整评审流程|行为埋点统计|
|Phase 2|形成用户留存|月活 ≥ 500，次月留存 ≥ 60%|Analytics 追踪|
|Phase 2|验证商业可行性|付费转化率 ≥ 5%（Pro 计划）|Stripe 支付数据|
|Phase 2|提升评审质量|用户上会通过率自述提升 ≥ 30%|用户反馈问卷|
---

## 9. 风险与应对

|级别|风险点|概率 × 影响|应对策略|
|---|---|---|---|
|高|AI 质疑质量不够尖锐，用户体验差|高 × 高|Prompt 工程迭代，引入真实评审案例 few-shot|
|高|每次评审 API 成本过高（>¥5/次）|中 × 高|控制 max_tokens，限制角色数，设置用量上限|
|中|PRD 文档过长导致 token 超限|高 × 中|前端截断 + 智能摘要，提示用户精简文档|
|中|多轮辩驳后角色永不通过（死循环）|中 × 中|设置最大轮次限制（10轮），超限自动判定通过|
|低|SSE 连接不稳定，长时间评审中断|低 × 中|断线重连机制，服务端结果持久化后可恢复|
> （注：文档部分内容可能由 AI 生成）