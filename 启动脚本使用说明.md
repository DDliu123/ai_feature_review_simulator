# AI需求评审模拟器 - 启动脚本使用说明

## 脚本列表

### 1. AI需求评审模拟器.bat (Windows 批处理版本)
- **适用系统**: Windows XP/7/8/10/11
- **特点**: 简单易用，不需要额外配置
- **使用方法**: 双击运行

### 2. AI需求评审模拟器.ps1 (PowerShell 版本)
- **适用系统**: Windows 7 及以上（需要 PowerShell 3.0+）
- **特点**: 功能更强大，错误处理更好，界面更美观
- **使用方法**: 右键点击文件，选择"使用 PowerShell 运行"

## 功能特性

✅ **自动端口检测** - 自动检测并处理端口冲突
✅ **一键启动** - 同时启动前端和后端服务
✅ **自动打开浏览器** - 服务启动后自动打开应用界面
✅ **自动安装依赖** - 首次运行自动安装 npm 依赖
✅ **服务状态检查** - 确保服务完全启动后再打开浏览器
✅ **优雅关闭** - 一键关闭所有服务

## 使用前准备

1. **安装 Node.js 和 npm**
   - 访问 [nodejs.org](https://nodejs.org/) 下载并安装
   - 安装完成后，在命令行运行 `node -v` 和 `npm -v` 验证

2. **确保项目结构完整**
   ```
   ai_feature_review_simulator/
   ├── backend/
   ├── feature-review-simulator/
   ├── AI需求评审模拟器.bat
   └── AI需求评审模拟器.ps1
   ```

## 使用方法

### 方法一：使用批处理脚本（推荐新手）
1. 双击 `AI需求评审模拟器.bat`
2. 等待脚本自动完成所有操作
3. 浏览器自动打开后，即可开始使用
4. 使用完成后，按任意键关闭所有服务

### 方法二：使用 PowerShell 脚本（推荐高级用户）
1. 右键点击 `AI需求评审模拟器.ps1`
2. 选择"使用 PowerShell 运行"
3. 如果提示执行策略问题，请以管理员身份运行 PowerShell，执行：
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```
4. 等待服务启动完成
5. 按 Ctrl+C 可关闭所有服务

## 故障排除

### 端口被占用
脚本会自动检测端口占用情况，并自动切换到可用端口：
- 后端端口：3001 → 3002 → 3003 ...
- 前端端口：5173 → 5174 → 5175 ...

### 服务启动失败
1. 检查是否已安装 Node.js 和 npm
2. 检查网络连接是否正常（需要下载依赖）
3. 查看弹出的命令窗口中的错误信息
4. 确保没有其他程序占用所需端口

### 浏览器未自动打开
1. 手动访问 http://localhost:5173（或脚本提示的其他端口）
2. 检查是否有安全软件阻止了脚本操作

### PowerShell 脚本无法运行
1. 检查 PowerShell 版本：`$PSVersionTable.PSVersion`
2. 确保版本在 3.0 以上
3. 设置执行策略（见上文）

## 端口检查工具

还提供了一个简单的 Node.js 端口检查工具：

```bash
node check-ports.js
```

这个工具会显示当前哪些端口可用，帮助诊断端口冲突问题。

## 注意事项

- **首次运行**会自动安装依赖，可能需要较长时间
- **关闭脚本**时，会自动清理所有相关进程
- **防火墙提示**时，请允许 Node.js 访问网络
- **杀毒软件**可能会误报，请将脚本添加到信任列表

## 技术支持

如果遇到问题，请：
1. 查看命令行窗口中的错误信息
2. 检查端口占用情况
3. 确保所有依赖已正确安装
4. 尝试使用其他版本的启动脚本

---

**祝您使用愉快！** 🚀