# 手动启动指南

如果自动脚本无法正常工作，请按照以下步骤手动启动：

## 步骤 1：检查环境

### 检查 Node.js 是否安装
```bash
node -v
npm -v
```

如果没有安装，请到 [nodejs.org](https://nodejs.org/) 下载安装。

## 步骤 2：安装依赖

### 安装后端依赖
```bash
cd backend
npm install
```

### 安装前端依赖
```bash
cd feature-review-simulator
npm install
```

## 步骤 3：启动后端服务

```bash
cd backend
npm run dev
```

你应该会看到类似输出：
```
🚀 Server ready at: http://localhost:3001
```

## 步骤 4：启动前端服务（新终端）

打开一个新的终端窗口，执行：

```bash
cd feature-review-simulator
npm run dev
```

你应该会看到类似输出：
```
➜  Local:   http://localhost:5173/
```

## 步骤 5：访问应用

打开浏览器，访问：http://localhost:5173

## 常见问题

### 端口被占用
如果提示端口被占用，可以修改端口：

**后端（backend/.env）：**
```
PORT=3002
```

**前端（feature-review-simulator/package.json）：**
```json
"scripts": {
  "dev": "vite --port 5174"
}
```

### 数据库连接失败
如果是数据库连接问题，检查 backend/.env 文件：
```
DATABASE_URL="file:./dev.db"
```

### 环境变量缺失
确保 backend 目录下有 .env 文件：
```
JWT_SECRET=your-secret-key
DATABASE_URL="file:./dev.db"
```

## 检查服务状态

### 检查后端是否运行
```bash
curl http://localhost:3001/health
```

应该返回：
```json
{"status":"ok"}
```

### 检查前端是否运行
在浏览器访问 http://localhost:5173 应该能看到登录页面。

## 关闭服务

在启动服务的终端中按 `Ctrl+C` 即可停止服务。

## 需要帮助？

如果按照以上步骤仍有问题，请：
1. 运行 `诊断问题.bat` 查看具体问题
2. 检查终端中的错误信息
3. 确保所有依赖都已正确安装
4. 检查防火墙和杀毒软件设置